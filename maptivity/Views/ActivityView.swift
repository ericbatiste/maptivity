import SwiftUI

struct ActivityView: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("Recorded Activity")
                    .padding()
            }
        }
    }
}
